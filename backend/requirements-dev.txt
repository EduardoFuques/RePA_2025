# Dependencias de DESARROLLO Y TEST. No se instalan en la imagen Docker.
#
# Para trabajar en local, o en CI:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Instalar solo este archivo no alcanza: los tests importan la aplicacion,
# asi que necesitan tambien las de runtime.

-r requirements.txt

# Linting
ruff>=0.16.8,<1.0.0

# Testing
pytest>=9.1.1,<10.0.0
pytest-asyncio>=1.4.0,<2.0.0
# httpx no se importa directo en ningun test, pero es lo que usa por debajo
# el TestClient de FastAPI (5 archivos de tests). Sin el, fallan todos.
httpx>=0.27.0,<1.0.0
testcontainers[postgres]>=4.15.0,<5.0.0
